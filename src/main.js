$ = jQuery = require('jquery');
var React = require('react');
var PhotoFeedComponent = require('./components/photoFeedComponent');

React.render(<PhotoFeedComponent />, document.getElementById('app'));
